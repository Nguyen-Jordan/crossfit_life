<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Réinitialisation de mot de passe{% endblock %} - Crossfit life</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <! -- Feuilles de styles -->
    <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/card/styleCard.css') }}">

</head>
<body>
    <section class="container my-4 p-3">
        <h1 class="texte">Réinitialisation de mot de passe</h1>
        <div class="row">
            <div class="col align-content-center mt-5">
                {{ form_start(requestPassForm) }}
                    {{ form_row(requestPassForm.email) }}
                    <button type="submit" class="btn btn-lg btn-primary mt-3">Envoyer</button>
                {{ form_end(requestPassForm) }}
            </div>
        </div>
    </section>
</body>