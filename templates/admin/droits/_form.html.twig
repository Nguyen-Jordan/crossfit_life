{{ form_start(droitForm) }}
  <div class="col-md-12 my-2">
    {{ form_row(droitForm.name) }}
  </div>
  <div class="col-md-8 my-2">
    <button type="submit" class="btn btn-lg btn-warning my-2">{{ button_label|default('Ajouter') }}</button>
  </div>
{{ form_end(droitForm) }}