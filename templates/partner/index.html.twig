{% extends 'base.html.twig' %}

{% block title %}{{ app.user.franchise.name }}- Crossfit_life{% endblock %}


{% block body %}
<div class="texte">{{ app.user.franchise.name }}</div>
<div class="conteneur">
  <section class="rang">
      {% for structure in app.user.franchise.structures %}
              <article class="carte">
                  <div class="en-têteCarte">
                      <h1>{{ structure.address }}</h1>
                  </div>
                  <div class="titre">
                      <h2>Les droits</h2>
                  </div>
                  <div class="corpsCarte">
                      <table class="table table-borderless">
                          <tbody>
                          {% for droit in structure.structuresDroits %}
                              <tr>
                                  <td>
                                      {{ droit.droits.name }}
                                  </td>
                                  <td>
                                      <div class="form-switch text-center">
                                          <input class="form-check-input" type="checkbox"
                                              {{ (droit.status) ? 'checked' : ''}}
                                              id="flexSwitchCheckDefault">
                                          <label class="form-check-label" for="flexSwitchCheckDefault"></label>
                                      </div>
                                  </td>
                              </tr>
                          {% endfor %}
                          </tbody>
                      </table>
                  </div>
              </article>
      {% endfor %}
  </section>
</div>
{% endblock %}